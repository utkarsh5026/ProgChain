FROM node:20-alpine3.20
WORKDIR /app

# Copy package files for caching the dependency installation step
COPY package*.json ./
RUN npm install

# Copy all source code
COPY . .

# Expose the port used by the Vite dev server (default: 5173)
EXPOSE 5173

# Start the Vite development server
CMD ["npm", "run", "dev"]